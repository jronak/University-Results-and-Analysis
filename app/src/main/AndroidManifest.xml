<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ronak.com.vtu_results" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Mainhome_activity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Results"
            android:label="@string/Live_string">
        </activity>
        <activity android:name=".Home"
            android:label="@string/Live_string">
           </activity>
        <activity
            android:name=".Schedule"
            android:label="Schedule">
            </activity>
        <service android:name=".Background_servcice">
        </service>
        <activity android:name=".Class_room"
            android:label="Class results">
            </activity>
        <activity android:name=".Class_results"
            >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".Analysis"
            android:label="Analysis">
            </activity>
    </application>

</manifest>
