<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="17sp"
        android:layout_margin="10dp"
        android:background="#fff"
        android:id="@+id/class_tv"
        android:text="Class results fetches the result of all the students belonging to the same class Ex:1CR13CS"
        android:padding="10dp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:id="@+id/class_ed"
        android:padding="10dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg"
        android:hint="@string/class_usn"
        android:gravity="center"
        android:inputType="textCapCharacters"
        android:layout_below="@+id/class_tv"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="@string/Getresults"
        android:background="@drawable/button_selector"
        android:onClick="getclass_results"
        android:id="@+id/getclass"
        android:textAllCaps="false"
        android:textColor="#fff1f1f1"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
