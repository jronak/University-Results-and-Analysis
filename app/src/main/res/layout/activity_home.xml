<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="17sp"
        android:layout_margin="10dp"
        android:background="#fff"
        android:id="@+id/tv"
        android:text="Live result tries to fetch the result from the generic server, if the result is unavailable set up Scheduled result check"
        android:padding="10dp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:id="@+id/USN_box"
        android:padding="10dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg"
        android:hint="@string/EnterUSN"
        android:gravity="center"
        android:inputType="textCapCharacters"
        android:layout_below="@+id/tv"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="@string/Getresults"
        android:background="@drawable/button_selector"
        android:onClick="getResults"
        android:id="@+id/getresults"
        android:textColor="#fff1f1f1"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
